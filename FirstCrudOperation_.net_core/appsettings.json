{
  "ConnectionStrings": {
    "DB": "Server=localhost;Database=db_FirstCrudOperation_.net_core; Integrated Security= True; TrustServerCertificate= True;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}







//Why the appsettings.json file is important in a .NET Core application?

// here we can the configuration settings for our application, such as database connection strings, 
//logging levels, and other application-specific settings. 
//The appsettings.json file allows us to easily manage 
//and change these settings without having to modify the code
//. It also supports different environments (e.g., Development, Production) by allowing us to create environment-specific configuration files (e.g., appsettings.Development.json) that can override the default settings in appsettings.json. This makes it easier to maintain and deploy applications across different environments. `
